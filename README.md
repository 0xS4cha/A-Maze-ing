# A Maze Ing

## TODO
- [ ] Use PEP 257 for docstrings
- [ ] Use type definition and check using `mypy`
- [ ] Makefile using rules `install` `run` `debug` `clean` `lint-strict` and `lint`
- [ ] Add a `.gitignore`
- [ ] Create test programs to verify project functionality
- [ ] Use venv
- [ ] Check if the input and output coordinates are within the width and height.